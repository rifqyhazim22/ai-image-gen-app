{
  "outputDirectory": "web_dist",
  "buildCommand": "echo \"skip build, using committed web_dist\"",
  "installCommand": "echo \"skip install\"",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
